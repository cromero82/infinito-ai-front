---
description: Ajuste de viewport para tablas y contenedores hijos con footer fijo, scrollDisabled e infinite scroll
globs: **/*-list*.component.ts, **/*list*.component.html, **/*list*.component.scss, **/financiero/**/*.component.html, **/financiero/**/*.component.scss
alwaysApply: false
---

# Ajuste de pantalla a tabla de datos - Registros visibles

**Usar esta regla solo cuando:** El usuario pida ajustar el número de registros visibles en una tabla/listado según la resolución de pantalla, o implementar infinite scroll en una tabla paginada.

## Problema
Tablas que cargan muchos registros o usan paginación fija no se adaptan al viewport. Resultado: se ven pocas filas, espacio vacío, o scroll innecesario.

## Solución reutilizable

### 1. Servicio: `TableViewportService`
Ubicación: `src/app/core/table-viewport/table-viewport.service.ts`

```ts
const v = this.tableViewportService.calculate({ reservedHeight: 388 });
this.pageSize = v.pageSize;
this.tableScrollMaxHeight = v.maxHeight;
```

**Opciones:** `reservedHeight` (espacio para navbar, header, tabs, filtros), `rowHeight` (default 28), `minHeight` (default 140), `minPageSize` (default 5).

### 2. Template
- Contenedor con `#tableScroll`, clase `table-scroll-container` y `[style.max-height.px]="tableScrollMaxHeight"`
- `overflow-y: auto` en el contenedor

### 3. Infinite scroll
- `@HostListener('window:resize')` → llamar `applyViewport()`
- Scroll listener: cuando `scrollTop + clientHeight >= scrollHeight - 100` → cargar siguiente página
- Añadir resultados a `dataSource` sin reemplazar
- `maybeLoadMoreIfNoScroll()`: si el contenido no llena el contenedor y hay más páginas, cargar más

### 4. CSS compartido
`src/app/core/table-viewport/table-viewport.scss`:
```scss
.table-scroll-container { overflow-y: auto; overflow-x: auto; }
```

### 5. Layout con tabs + footer fijo (`scrollDisabled`)
- Cuando la ruta usa `data: { scrollDisabled: true }`, no usar alturas rígidas tipo `height: calc(100vh - ...)` dentro del componente hijo.
- El patrón base debe ser:
  - `:host { display:flex; flex-direction:column; flex:1 1 auto; min-height:0; min-width:0; }`
  - `*-page { flex:1 1 auto; min-height:0; }`
  - wrapper interno `flex flex-col flex-1 min-h-0 w-full`
  - `card ... flex flex-col flex-1 min-h-0 overflow-hidden`
- El contenedor principal de datos debe usar `flex: 1 1 auto`, `min-height: 0` y `overflow: hidden/auto` para que el espacio libre se convierta en altura útil de tabla, no en hueco antes del footer.
- Si hay panel dividido, usar:
  - `.split-pane-container { display:flex; flex:1 1 auto; min-height:0; height:100%; overflow:hidden; }`
  - panel derecho/listado `display:flex; flex-direction:column; min-height:0`
  - tabla/detalle final `flex:1 1 auto; min-height:0; overflow:auto`
- La separación vertical entre tabs del contenedor padre y el componente hijo debe replicarse arriba y abajo con un valor pequeño y consistente (`mt-1` / padding inferior equivalente), evitando `pb` grandes en el padre.

## Ejemplo de referencia
`src/app/pages/apps/financiero/egresos/egreso-list/egreso-list.component.ts`
