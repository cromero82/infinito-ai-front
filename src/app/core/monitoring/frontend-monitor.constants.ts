/** Fragmento de URL del endpoint de reporte (excluir de logs y de re-reporte). */
export const REPORTE_FRONTEND_URL_FRAGMENT = 'reporte-frontend';

/** Máximo de líneas de actividad reciente en memoria. */
export const FRONTEND_MONITOR_MAX_ACTIONS = 10;

/** Tamaño máximo aproximado de JSON serializado en una línea de actividad. */
export const FRONTEND_MONITOR_MAX_JSON_CHARS = 1500;
