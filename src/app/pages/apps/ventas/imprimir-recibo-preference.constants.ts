/** Clave en `localStorage` para persistir si se imprime el ticket tras pagar (`'true'` | `'false'`). */
export const IMPRIMIR_RECIBO_KEY = 'imprimir-recibo';

/** Texto unificado para el toggle del menú de tickets y el checkbox en pago en efectivo. */
export const IMPRIMIR_TICKET_LUEGO_DE_PAGAR_LABEL =
  'Imprimir ticket luego de pagar';
